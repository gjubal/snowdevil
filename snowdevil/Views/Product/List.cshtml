@using snowdevil.Models.ViewModels
@model ProductsListViewModel
@foreach (var p in Model.Products)
{
    @Html.Partial("ProductSummary", p)
} 

<div page-model="@Model.PagingInfo" page-action="List" page-classes-enabled="true" page-class="button" page-class-normal="is-light" page-class-selected="is-info is-light" page-url-category="@Model.CurrentCategory"></div>

